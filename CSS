*{
    margin:0;
    padding: 0;
    text-align: center;
    background-color:rgb(57, 99, 99) ; 
}h1{
    background-color: aqua;
    color: #fff;
    height: 5rem;
    line-height: 5rem;
}
.choice{
    height: 165px;
    width: 165px;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
}
.choice:hover{
    cursor: pointer;
    background-color: #081b31;
}
img{
    height: 150px;
    width: 150px;
    object-fit: cover;
    border-radius: 50%;
}
.choices{
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 3rem;
    margin-top:5rem
}

.score-board {
    text-align: center; /* Centers the scoreboard on the page */
    margin-top: 20px; /* Adds some space at the top of the scoreboard */
}

.score {
    display: flex; /* Uses Flexbox to layout children */
    justify-content: center; /* Centers the children horizontally in the flex container */
    align-items: center; /* Aligns items vertically in the center */
    gap: 20px; /* Adds space between the "You" and "Comp" sections */
}

.label, #user-score, #comp-score {
    margin: 5px; /* Adds a small margin around each paragraph for spacing */
    font-size: 1.5rem; /* Sets a standard font size for the text */
}
.you{
    font-size: 1.5rem;
}
.comp{
    font-size: 1.5rem;
}

.label {
    font-weight: bold; /* Makes the "You" and "Comp" labels bold */
}

#user-score, #comp-score {
    min-width: 50px; /* Ensures a minimum width for score elements */
    background-color: #f4f4f4; /* Light grey background for the scores */
    padding: 10px; /* Pads the scores for better visibility */
    border-radius: 5px; /* Rounds the corners of the score boxes */
}



/*
.score-board{
   display: flex;
   justify-content: center;
   align-items: center;
    font-size: 2rem;
    gap: 1rem;
    margin-top:3rem
}
.score
{
    display: flex;
}
#user-score,
#comp-score{
    font-size: 3.5rem;   
}*/
.msg-container{
    margin-top: 5rem;
}
#msg{
    margin-top: 5rem;
    background-color:#081b31;
    color: #fff;
    font-size: 2rem;
    display: inline;
    padding:1rem ;
    border-radius: 1rem;
}
